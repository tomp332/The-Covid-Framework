version: '3'

services:

  backend:
    container_name: backend
    restart: always
    image: "ghcr.io/pako332/covid-framework/server-local:latest"
    ports:
      - "3000:3000"
  #environment:
      #- HOST=localhost # Enter your own local ip
      #- ATLAS_URI_MONGO=mongodb+srv://admin:RRTGwiGAwoSzem8K@covidcluster.spwpg.mongodb.net/CovidFramework?retryWrites=true&w=majority # Enter your own mongo DB url

  frontend:
    restart: always
    container_name: frontend
    image: "ghcr.io/pako332/covid-framework/app-local:latest"
    ports:
      - "443:443"
    environment:
      #- REACT_APP_API_URL=covidframework.com # Enter your own local ip
      #- ATLAS_URI_MONGO=mongodb+srv://admin:RRTGwiGAwoSzem8K@covidcluster.spwpg.mongodb.net/CovidFramework?retryWrites=true&w=majority # Enter your own mongo DB url
    depends_on:
      - backend
